{
  "name": "ai-apply-agents",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/client", "apps/server", "apps/landing", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "dev:reset": "(bunx kill-port 8000 || true) && (bunx kill-port 8001 || true) && (bunx kill-port 8002 || true) && turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@types/file-saver": "^2.0.7",
    "turbo": "^2.8.17"
  },
  "packageManager": "bun@1.3.10",
  "dependencies": {
    "docx": "^9.6.1",
    "file-saver": "^2.0.5",
    "html2canvas-pro": "^2.0.2",
    "jspdf": "^4.2.1"
  }
}
