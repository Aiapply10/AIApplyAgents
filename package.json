{
  "name": "ai-apply-agents",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/client", "apps/server", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "dev:reset": "(bunx kill-port 8000 || true) && (bunx kill-port 8001 || true) && (bunx kill-port 8002 || true) && turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.8.17"
  },
  "packageManager": "bun@1.3.10"
}
