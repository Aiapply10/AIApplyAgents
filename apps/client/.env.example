# Copy to .env.local for local development
# Only VITE_* variables are exposed to the client
# VITE_API_URL=http://localhost:8000

# Client does not connect to MongoDB; API calls go to the control service
