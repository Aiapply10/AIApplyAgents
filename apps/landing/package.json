{
  "name": "@repo/landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "vite build",
    "preview": "vite preview --port 3000",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "vite": "^6.0.3"
  }
}
