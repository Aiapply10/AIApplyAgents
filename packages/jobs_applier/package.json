{
  "name": "@repo/jobs-applier",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run --package jobs-applier uvicorn main:app --app-dir src --host 0.0.0.0 --port 8002 --reload",
    "build": "uv run --package jobs-applier python -c \"pass\"",
    "lint": "uv run --package jobs-applier ruff check .",
    "clean": "echo 'no-op'"
  }
}
