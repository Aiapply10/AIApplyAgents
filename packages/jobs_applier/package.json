{
  "name": "@repo/jobs-applier",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run --package jobs-applier fastapi dev src/main.py --port 8002",
    "build": "uv run --package jobs-applier python -c \"pass\"",
    "lint": "uv run --package jobs-applier ruff check .",
    "clean": "echo 'no-op'"
  }
}
